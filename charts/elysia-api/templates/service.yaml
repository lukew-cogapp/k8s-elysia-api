apiVersion: v1
kind: Service
metadata:
  name: { { include "elysia-api.fullname" . } }
spec:
  selector:
    app: { { include "elysia-api.name" . } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.port } }
